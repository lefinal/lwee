# Stdin

`stdin`

Provides data via standard input to the operation.
The stream will be closed once all input data is consumed.
This input type is the easiest to use and has a wide range of support for other programming languages and applications.

## Configuration

```yaml
source: ""
provideAs: stdin
```
